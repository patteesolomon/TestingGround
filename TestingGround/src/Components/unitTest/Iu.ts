import React from 'react';
// @param {number} x
// @return {boolean}
let counter = 0;
let rev: String;
const Iu = (x: number) => {
    rev = new String(x); // last iter
let u = 0; // first iter
    counter = rev.length;
}

export default Iu